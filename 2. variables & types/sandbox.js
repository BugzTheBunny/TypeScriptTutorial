var character = "mario";
var age = 30;
var isBlackBelt = false;
character = "Luigi"; // this will work
var circ = function (diameter) {
    return diameter * Math.PI;
};
console.log(circ(7.5));
var names = ['Luigi', 'Yoshi', 'Mario'];
names.push('Bunny');
// names.push(5) // This will not work.
// names[0] = 5 // This will also not work
var numbers = [1, 2, 3, 4];
numbers.push(5);
// numbers.push('Hello') // This will not work
// numbers[0] = 'Hello' // This will not work
var mixed_array = ['Yoshi', 20, 'Thomas', 40];
mixed_array.push(5);
mixed_array.push('Bunny');
var person = {
    name: 'Josh',
    age: 45,
    job: 'Developer',
    is_working: true
};
// person.age = "Ten" // This will not work
// person.skills = ['Java','Python','TypeScript']
person = {
    name: 'Tom',
    age: 35,
    job: 'Developer',
    is_working: true
};
var person_name;
var person_age;
var person_logged;
var state;
state = 'Hello';
state = 10;
var people;
var ages;
var logged;
var items;
items.push('Hi');
items.push(15);
// items.push(true) // This will not work
var human;
human = {
    name: 'Bob',
    age: 15
};
// human = 10 // this will not work
var alien;
var anything;
anything = {};
anything = [];
anything = 'Hi';
anything = 10;
